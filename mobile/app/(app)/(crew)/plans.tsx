/**
 * Expo Router stub — canonical Plans UI lives in screens/Plans.tsx (App.tsx stack).
 * Keep this route aligned with tier packages so deep links do not show the old couple model.
 */
import Plans from '@/screens/Plans';

export default function CrewPlansScreen() {
  return <Plans />;
}
